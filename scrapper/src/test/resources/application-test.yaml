app:
  github:
    token: test
  stackoverflow:
    key: test
    access-token: test
  bot:
    base-url: http://localhost:8080
  scheduler:
    interval: 1h
  database:
    access-type: SQL

spring:
  jpa:
    hibernate:
      ddl-auto: validate
